dist
node_modules
coverage
src/types/supabase.ts

# Local-only artifacts. Not in the repo, but `npm run format:check` runs over
# the working tree, so without these a dev with a linked Supabase project or a
# graphify run gets a red check for files they never wrote.
supabase/.temp
graphify-out

# Prose is not prettier's job (#382): formatting markdown rides unrelated
# emphasis and blank-line churn into doc-touching PRs. Also protects
# openwiki/ (regenerated by a workflow) and .agents/skills/ (vendored).
*.md
