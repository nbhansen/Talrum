node_modules
dist
dist-ssr
coverage
*.local
.vite
.DS_Store
# Every env file is local-only; the *.example templates are the committed ones.
.env*
!.env.example
!.env.cloud.example
*.log
*.tsbuildinfo

# Local-only agent operating instructions (per-machine)
CLAUDE.md
GEMINI.md

# Local agent / MCP harness state — per-machine, may contain secrets.
# .claude/settings.json is the shared exception: it holds the repo hooks.
.agents/
.claude/*
!.claude/settings.json
.mcp.json
skills-lock.json

# Git worktrees (isolated workspaces for parallel branches)
.worktrees/

# Graphify output (local knowledge graph artifacts)
graphify-out/

# Temp fixture of scripts/verify-boundaries.mjs; left behind only on SIGKILL
src/ui/boundary-canary-*/
